<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matthieu.minipoll">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConnectionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".Profile.CreateProfilActivity" />
        <activity android:name=".PollListeActivity" />
        <activity android:name=".SondageViewActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".FriendActivity" />
        <activity android:name=".New_poll.NewPollActivity" />
        <activity android:name=".AddFriend" />
        <activity android:name=".New_poll.NewChoiceActivity" />
        <activity android:name=".ChoiceOfPictureActivity"
            android:theme="@style/AppTheme.CustomTheme"
            />
        <activity android:name=".New_poll.NewSondageActivity" />
        <activity android:name=".New_poll.NewSondageTempoActivity" />
        <activity android:name=".New_poll.ListeAmisPourPollActivity" />
        <activity
            android:name=".New_poll.NewQuizzActivity"
            android:label="@string/title_activity_new_quizz"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Profile.EditProfileActivity">

        </activity>
    </application>

</manifest>